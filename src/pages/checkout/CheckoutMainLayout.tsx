import React from 'react';
import { ReactElement } from 'react';
import Checkout from './Checkout';

const CheckoutMainLayout = (): ReactElement => {
  return <Checkout />;
};
export default CheckoutMainLayout;
